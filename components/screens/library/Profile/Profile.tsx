import { View, Text } from "react-native";
import React, { FC } from "react";

const Profile: FC = () => {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
};

export default Profile;
